import 'package:decimal/decimal.dart';

/// P = n(RT/V)
Decimal _computePressure() {
  return Decimal.zero;
}

/// V = n(RT)/P
Decimal _computeVolume() {
  return Decimal.zero;
}

/// n = (PV)/(RT)
Decimal _computeAmountOfSubstance() {
  return Decimal.zero;
}

/// T = (PV)/(nR)
Decimal _computeTemperature() {
  return Decimal.zero;
}